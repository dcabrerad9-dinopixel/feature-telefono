import Container from "../components/Container";

const TextField = () => {
  return (
    <Container title="TextField" path="home / componentes / TextField">
      <div>
        <p>Text Field</p>
      </div>
    </Container>
  );
};

export default TextField;